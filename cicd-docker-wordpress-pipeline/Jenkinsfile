pipeline {
    agent any

    environment {
        REPO_URL = 'https://github.com/Asaf01/ci'
        IMAGE_NAME = 'wordpress'
        DOCKERFILE_DIR = 'cicd-docker-wordpress-pipeline'
        DOCKERHUB_CREDENTIALS = 'dockerhub_login'  // Ensure this matches the ID of your credentials
        DOCKERHUB_REPO = 'bendrorasaf/wordpress'
    }

    stages {
        stage('Clone Repository') {
            steps {
                // Checkout the repository
                git branch: 'main', url: "${REPO_URL}"
            }
        }
        
        stage('Determine Version') {
            steps {
                script {
                    // Fetch the latest version from Docker Hub and increment it
                    def latestTag = sh(
                        script: "docker run --rm appropriate/curl -s https://hub.docker.com/v2/repositories/${DOCKERHUB_REPO}/tags/?page_size=1 | docker run --rm stedolan/jq -r '.results[0].name'",
                        returnStdout: true
                    ).trim()

                    // Extract the numeric part and increment it
                    def versionNumber = latestTag ? latestTag.replaceAll('v', '').toInteger() + 1 : 1
                    env.VERSION = "v${versionNumber}"
                }
            }
        }
        
        stage('Build Docker Image') {
            steps {
                // Build the Docker image
                script {
                    dockerImage = docker.build("${DOCKERHUB_REPO}:${VERSION}", "${DOCKERFILE_DIR}")
                }
            }
        }

        stage('Test Docker Image') {
            steps {
                // Run a container from the image and execute a test command
                script {
                    sh 'echo "Testing the Docker image..."'
                    // Add your actual test commands here
                }
            }
        }
        
        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', "${DOCKERHUB_CREDENTIALS}") {
                        dockerImage.push("${VERSION}")
                        dockerImage.push('latest')  // Optionally push the image with the 'latest' tag
                    }
                }
            }
        }

        stage('Delete Workspace') {
            steps {
                // Cleanup workspace
                deleteDir()
            }
        }
    }

    post {
        always {
            // Ensure workspace is cleaned up after the build
            cleanWs()
        }
    }
}
